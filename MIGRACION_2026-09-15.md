# Migración del 15/09/2026 (para pegar en la Biblia cuando la carpeta Desktop/cohaj esté conectada)

- **Supabase**: nuevo proyecto **Cohaj**, ref `uenfocgoctlbmdhcalfo`, región **sa-east-1 (São Paulo)**, cómputo Micro (incluido en el Pro de la org Ventrix). Reemplaza a `coaj` (`bqujsvrfcyqazzjysijn`, Oregon), que queda **como respaldo hasta fin de mes** y después se puede pausar/borrar.
- Cómo se migró: esquema con los SQL del repo en orden (schema.sql → reparto_ddl → stock → permisos → suspension_dias → feriados_parciales → devoluciones_importe → altas_suscripciones → seguridad (sin kv) → funciones_finales); datos copiados con `postgres_fdw` desde el proyecto viejo (pooler us-west-2, usuario postgres.<ref>, contraseña reseteada ese día — **Demian tiene que volver a resetearla**); tablas copiadas con triggers de auditoría apagados y secuencias reajustadas; `auth.users` + `auth.identities` copiados con los mismos ids y contraseñas cifradas (los 2 usuarios entran con su clave de siempre). Verificado: mismas cantidades en las 22 tablas y misma suma de saldos de los 166 activos ($3.355.450). La tabla `kv` (respaldo JSON viejo) NO se migró.
- El proyecto nuevo se creó con "Automatically expose new tables" apagado → hubo que dar `grant` explícito a `authenticated` (select/insert/update/delete en tablas, usage en secuencias, execute en funciones) + default privileges. `anon` sin permisos. Signups apagados. Edge Function `usuarios` desplegada desde el editor del dashboard (mismo código del repo).
- **App**: `SB_URL` = `https://uenfocgoctlbmdhcalfo.supabase.co`, `SB_KEY` = **publishable key** (`sb_publishable_…`, no JWT) con supabase-js 2.49.4 (probado). v66.
- **Hosting**: además de GitHub Pages, el repo está conectado a **Vercel** (equipo demian, proyecto `cohaj`): **https://cohaj.vercel.app** — se redeploya solo con cada push a main. GitHub Pages sigue funcionando en paralelo. Dominio propio: Vercel → cohaj → Settings → Domains → Add.
- La carpeta local `Desktop/coaj` pasó a llamarse **`Desktop/cohaj`** (Biblia y archivos de referencia adentro).
- Pendiente: Demian debe resetear la contraseña de la base del proyecto viejo (Database → Settings → Reset database password) y, si quiere, activar "leaked password protection" en Auth del proyecto nuevo.
